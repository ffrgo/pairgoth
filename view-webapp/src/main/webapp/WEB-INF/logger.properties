# Per-logger levels for webapp-slf4j-logger (overrides the global logger.level).
# The /api/events SSE proxy pulls in Jetty's HttpClient, which is extremely chatty at the
# webapp's trace level; keep its whole tree quiet (level resolution is hierarchical).
org.eclipse.jetty.level = warn
