# webapp
webapp.env = dev
webapp.url = http://localhost:8080

# smtp
smtp.sender = 
smtp.host = 
smtp.port = 587
smtp.user = 
smtp.password = 

# logging
logger.level = trace

# display
# Show pairings as "Black vs White" instead of the default "White vs Black".
# Country/club-level convention; rarely needs to be set per user.
# display.pairing.blackFirst = false
